# Smart Contract Review System

This repository hosts an in-progress Legal AI/NLP pipeline that ingests contract PDFs, breaks them into clauses, classifies each clause with a fine-tuned LegalBERT model, and experiments with RAG-powered rewrites plus side-by-side comparisons. For now, this README is just a quick reference to capture the project idea; a full document will follow once the system is feature-complete.
